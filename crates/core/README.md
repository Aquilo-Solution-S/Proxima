# proxima-core

Runtime framework core: graph contracts, build-time registries, protocol verbs,
wake/personality runtime, MCP substrate, and storage ports.

Most applications should depend on `proxima`, not this crate directly. The Lean
kernel in [`../../docs/lean/Causa`](../../docs/lean/Causa) is the invariant authority, not this crate boundary.
