# proxima-mcp-server

MCP transport layer and generated self-documentation resources used by the
headless binary and embedded hosts.

Important resources: `proxima://how-to`, `proxima://tools`, memory resources,
and event polling templates.
